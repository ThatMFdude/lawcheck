services:
  - type: web
    name: lawcheck
    runtime: python
    buildCommand: pip install -r backend/requirements.txt && cd frontend && npm install && npm run build && cp -r build/* ../backend/
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        value: AIzaSyBcNigT6L3f02ufjmrY42eMiOYIdHbQ_Ws